"use client";

import Link from "next/link";
import { FormEvent, useState } from "react";
import { useCloud } from "@/components/cloud/CloudProvider";

export function AuthScreen({ initialMode }: { initialMode: "login" | "register" }) {
  const { user, loading, login, register } = useCloud();
  const [mode, setMode] = useState(initialMode); const [email,setEmail]=useState(""); const [username,setUsername]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [submitting,setSubmitting]=useState(false);
  const submit=async(event:FormEvent)=>{event.preventDefault();setSubmitting(true);setError("");try{if(mode==="login")await login(email,password);else await register(email,password,username);}catch(caught){setError(caught instanceof Error?caught.message:"Connexion impossible.");}finally{setSubmitting(false);}};
  return <main className="mx-auto min-h-screen max-w-lg px-4 py-5 sm:px-7"><header className="mb-8 flex items-center justify-between border-b border-[var(--line)] pb-4"><Link href="/" className="font-bold text-[var(--muted)] hover:text-white">← Accueil</Link><span className="text-sm font-black tracking-[.14em]">DARTFLOW CLOUD</span></header>{loading?<p className="text-[var(--muted)]">Chargement…</p>:user?<section className="rounded-[1.6rem] border border-[var(--lime)]/50 bg-[var(--panel)] p-6 text-center"><p className="text-xs font-black uppercase tracking-[.18em] text-[var(--lime)]">Connecté</p><h1 className="mt-2 text-3xl font-black">@{user.username}</h1><Link href="/account" className="mt-6 grid min-h-12 place-items-center rounded-xl bg-[var(--lime)] font-black text-black">Ouvrir mon compte</Link></section>:<section className="rounded-[1.6rem] border border-[var(--line)] bg-[var(--panel)] p-5"><div className="grid grid-cols-2 gap-2 rounded-2xl bg-black/20 p-1.5"><button type="button" onClick={()=>{setMode("login");setError("");}} className={`min-h-11 rounded-xl font-black ${mode==="login"?"bg-[var(--lime)] text-black":"text-[var(--muted)]"}`}>Connexion</button><button type="button" onClick={()=>{setMode("register");setError("");}} className={`min-h-11 rounded-xl font-black ${mode==="register"?"bg-[var(--lime)] text-black":"text-[var(--muted)]"}`}>Inscription</button></div><h1 className="mt-6 text-3xl font-black">{mode==="login"?"Bon retour !":"Créer mon compte"}</h1><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{mode==="login"?"Retrouve tes profils et parties sur tous tes appareils.":"Tes données locales seront associées à ton nouveau compte."}</p><form onSubmit={submit} className="mt-5 space-y-3">{mode==="register"&&<label className="block"><span className="mb-1 block text-sm font-bold">Nom d’utilisateur public</span><div className="flex min-h-12 items-center rounded-xl border border-[var(--line)] bg-[#0d0f0e] px-4"><span className="text-[var(--muted)]">@</span><input required minLength={3} maxLength={24} pattern="[A-Za-z0-9_]+" value={username} onChange={event=>setUsername(event.target.value)} autoComplete="username" className="min-w-0 flex-1 bg-transparent px-1 outline-none" /></div></label>}<label className="block"><span className="mb-1 block text-sm font-bold">Adresse email</span><input type="email" autoComplete="email" required value={email} onChange={event=>setEmail(event.target.value)} className="min-h-12 w-full rounded-xl border border-[var(--line)] bg-[#0d0f0e] px-4" /></label><label className="block"><span className="mb-1 block text-sm font-bold">Mot de passe</span><input type="password" autoComplete={mode==="login"?"current-password":"new-password"} minLength={8} required value={password} onChange={event=>setPassword(event.target.value)} className="min-h-12 w-full rounded-xl border border-[var(--line)] bg-[#0d0f0e] px-4" /></label>{error&&<p role="alert" className="text-sm font-bold text-[#ff9b7a]">{error}</p>}<button disabled={submitting} className="min-h-13 w-full rounded-xl bg-[var(--lime)] font-black text-black disabled:opacity-50">{submitting?"Patiente…":mode==="login"?"Se connecter":"Créer mon compte"}</button></form></section>}</main>;
}
