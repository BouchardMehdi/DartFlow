{
  "name": "dartflow",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "front",
    "back",
    "shared"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev:front": "npm run dev --workspace @dartflow/front",
    "dev:back": "npm run dev --workspace @dartflow/back",
    "build": "npm run build --workspace @dartflow/shared && npm run build --workspace @dartflow/back && npm run build --workspace @dartflow/front",
    "test": "npm run test --workspace @dartflow/front && npm run test --workspace @dartflow/back",
    "typecheck": "npm run build --workspace @dartflow/shared && npm run typecheck --workspace @dartflow/back && npm run typecheck --workspace @dartflow/front",
    "lint": "npm run lint --workspace @dartflow/back && npm run lint --workspace @dartflow/front"
  }
}
